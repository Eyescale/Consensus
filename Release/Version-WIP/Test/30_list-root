:
	on init
		do : list : (( base, toto ), titi )
		do ((( algae, plancton ), lichen ), garbage )
	else on list
		do > "list: %_\n": %((?,...):*list)
		do > "root: %_\n": %((?,...):*list):~(.,.)
		do exit
