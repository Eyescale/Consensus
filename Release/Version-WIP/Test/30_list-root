:
	on init
		do : list : (( base, toto ), titi )
		do ((( algae, plancton ), lichen ), garbage )
	else
		do > "genealogy: %_\n": %((?,...):*list)
		do > "root: %_\n": %((?,...):*list):~(.,.)
		do exit
