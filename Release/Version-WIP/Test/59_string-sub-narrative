
	on init
		do : string : "hello, world"
	else
		en *string

:"hello, world"
	on ( this )
		do >: this
		do exit

/* Note: the following works as well, and no strcmp

: %((*,string), ? )
	on ( this )
		do >: this
		do exit
*/
