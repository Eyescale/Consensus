
	on init
		do !! func()
#define Q_SRC
#ifndef Q_SRC
	else on ( hello, ? ) < .
//		do >:%<?>
		do >:%<(!,?)>
		do exit
#else
	else on hello < ?
#		do >: %<!>
#		do >: %<((!,.),?)>
		do >: %<(!,?:...)>
		do exit
#endif
: func
	on init
		do (( hello, world ), tti )
		do exit

