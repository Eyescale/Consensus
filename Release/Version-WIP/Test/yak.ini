#	Yak scheme definition file:
#		(( Rule, identifier ), ( Schema, (a,(b,...((%,id),...(z,'\0')))) ))
#		(( Rule, base ), ( Schema, ((%,identifier),'\0') ))
#
#	Assumed to be used in conjunction with yak.story, as
#		./B% -f Schemes/yak.ini yak < input

	(( Rule, really ), ( Schema, {
		'\0',
		(v,(e,(r,(y,'\0'))))
	} ))
	(( Rule, pass ), ( Schema, ((%,really),(' ',(g,(o,(o,(d,'\0')))))) ))
	(( Rule, base ), ( Schema, ((%,pass),'\0') ))
