
	!! $.story()
	on event < cgi >: . !!
		in ( : Leave-is->action: %event, : Home-is->view: %event )
			do exit
			/
		else in ( : Edit-is->action: %event, view: ?-is->view: %event )
			do >: $< file://Edit.hcn ></>
			/
		else
			do >:$< file://Home.hcn ></>
			/
		/
	/
